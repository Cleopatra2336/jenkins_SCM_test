pipeline {
  agent none
  stages {
    stage("Prepare container") {
      agent {
        docker {
          image 'openjdk:11.0.5-slim'
        }
      }
    }
    stage ("deploy"){
      
    }
  }
}
